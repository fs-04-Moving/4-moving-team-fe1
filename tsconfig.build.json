{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmitOnError": false, // 타입 에러 있어도 JS로 변환은 계속
    "skipLibCheck": true, // 외부 라이브러리 타입 무시 (속도 ↑, 에러 ↓)
    "incremental": true // 캐시 활용해서 빌드 속도 ↑
  },
  "include": ["app", "types"],
  "exclude": ["app/(providers)/(root)/test"]
}
