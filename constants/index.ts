import { FilterReceivedEstimateList } from '@/types/dtos/estimate.dto';

const RECEIVED_ESTIMATES: FilterReceivedEstimateList[] = [
  '전체',
  '확정한 견적서',
];

const constantOptions = {
  RECEIVED_ESTIMATES,
};

export default constantOptions;
